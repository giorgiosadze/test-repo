name: Create and compile a hello world C++ program
on: [push]
jobs:
  Explore-GitHub-Actions:
    runs-on: ubuntu-latest
    steps:
      - name: Create source
        run: printf "#include <iostream> \n int main() { std::cout << \"test\" << std::endl; } " > test.cpp
      - name: Compile source
        run: g++ test.cpp
      - run: ./a.out

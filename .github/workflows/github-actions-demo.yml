name: Build
on: [push]
jobs:
  Explore-GitHub-Actions:
    runs-on: ubuntu-latest
    steps:
      - name: Check out
        uses: actions/checkout@master
      - name: Compile source
        run: g++ main.cpp
      - run: ./a.out

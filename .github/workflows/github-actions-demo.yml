name: Build
on: [push]
jobs:
  Explore-GitHub-Actions:
    runs-on: ubuntu-22.04
    steps:
      - name: Check out
        uses: actions/checkout@master
      - name: Compile source
        run: g++ -std=c++20 main.cpp
      - run: ./a.out

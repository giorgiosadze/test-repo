name: Create and compile a hello world C++ program
on: [push]
jobs:
  Explore-GitHub-Actions:
    runs-on: ubuntu-latest
    steps:
      - name: Create source
        run: ls
      - name: Compile source
        run: g++ main.cpp
      - run: ./a.out
